require 'rails_helper'

RSpec.describe "departments/new", type: :view do

  # before(:each) { assign(:department, Department.new) }

  # specify { expect(render).to have_css '#main-menu', count: 1 }
  # specify{ expect(render).to have_css 'form input[type="submit"]', count: 1 }
  # specify{ expect(render).to have_css 'form input[type="text"]' }

end
