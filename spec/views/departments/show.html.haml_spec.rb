require 'rails_helper'

RSpec.describe "departments/show", type: :view do

  # fixtures :departments

  # let(:dept) { departments(:sales) }

  # before(:each) { assign(:department, dept) }

  # specify { expect(render).to have_css '#main-menu', count: 1 }
  # specify { expect(render).to have_text dept.name, count: 1 }

end
