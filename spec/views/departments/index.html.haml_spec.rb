require 'rails_helper'

RSpec.describe "departments/index", type: :view do

  # fixtures :departments

  # let(:dept) { Department.first }

  # before(:each) { assign(:departments, Department.all) }

  # specify { expect(render).to have_css '#main-menu', count: 1 }
  # specify { expect(render).to have_css '.page-title', text: /.{2,}/ }
  # specify { expect(render).to have_css 'table tr', text: dept.name }

end
